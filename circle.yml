machine:
  python:
    version: 3.5.2
dependencies:
  pre:
    - pip install -r requirements.txt
test:
  override:
    - python manage.py db migrate
    - python manage.py db upgradedb
    - python manage.py tests
deployment:
  staging:
    branch: develop
    commands:
